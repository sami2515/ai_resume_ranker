[pytest]
; Real NLP parsing (spaCy) of a real resume takes ~1-1.5s; a few tests
; parse 15-30 real resumes in one go, so this needs headroom above that,
; not just above a typical unit test.
timeout = 300
